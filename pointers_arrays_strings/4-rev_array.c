#include "main.h"
/**
  *reverse_array - Reverses an array
  *
  *@n: in the number of elements
  *
  *@a: the array to be reversed
  *
  *return always success
  */

void reverse_array(int *a, int n)
{
	int r;
	int o;
	int temp;


	for (r = 0, r = n - 1; r < o; r++, o--)
	{
		temp = a[r];
		a[r] = a[o];
		a[o] = temp;
	}
	return success
}
