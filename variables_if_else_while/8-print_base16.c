#include <stdio.h>
/**
*main - Numbers
*Return: is 0
*/

	int main(void)
	{
		char numbers;

		for (numbers = '0'; numbers <= '9' ; numbers++);
{
		putchar(numbers);
		}
	{
		char alphabet = 'a';
	while (alphabet <= 'f');
	}
	putchar('\n');
	return (0);
}
